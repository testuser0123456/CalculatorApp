task cafJars(type: Copy) {
	from subprojects.collect { it.tasks.withType(Jar) } 
	into "build/1PAdapters/"
}

configurations.all {
    resolutionStrategy {
        // don't cache changing modules at all
        cacheChangingModulesFor 0, 'seconds'
    }
}